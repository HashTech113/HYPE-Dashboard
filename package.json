{
  "name": "attendance-dashboard",
  "private": true,
  "scripts": {
    "dev": "concurrently -n BACKEND,FRONTEND -c green,blue \"bash backend/start.sh\" \"npm run dev --prefix frontend\"",
    "dev:backend": "bash backend/start.sh",
    "dev:frontend": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
